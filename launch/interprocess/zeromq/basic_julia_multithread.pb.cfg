# Configuration for the Julia multithread example. Unlike the C++ examples, the
# Julia applications require a configuration file as their only argument
# (see Goby.read_cli_cfg()), so one is provided here.
app {
    name: "basic_julia_multithread"
}
